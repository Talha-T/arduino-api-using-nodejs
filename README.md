# arduino-api-using-nodejs
Send data to your API using node.js and Arduino
## Installation
On a shell.
`npm install`
Then, with two seperate terminals, `node server/index.js` and then `node client/index.js`
